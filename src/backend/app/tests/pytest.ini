[pytest]
# Establece el directorio raíz del proyecto donde se encuentran los módulos de la aplicación
pythonpath = src

# Este directorio contiene tus pruebas (en este caso 'tests')
testpaths = src/backend/

# Otras configuraciones que podrías necesitar
addopts = --maxfail=1 --disable-warnings --strict
